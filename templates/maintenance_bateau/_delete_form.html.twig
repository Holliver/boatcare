<form method="post" action="{{ path('maintenance_bateau_delete', {'id': maintenance_bateau.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cette maintenance?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maintenance_bateau.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

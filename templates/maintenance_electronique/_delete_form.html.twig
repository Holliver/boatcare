<form method="post" action="{{ path('maintenance_electronique_delete', {'id': maintenance_electronique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maintenance_electronique.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

<form method="post" action="{{ path('doc_electricite_delete', {'id': doc_electricite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ doc_electricite.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

<div class="container">
    <div class="row">
        <div class="col">
            <form method="post" action="{{ path('maintenance_voile_delete', {'id': maintenance_voile.id}) }}"
                  onsubmit="return confirm('Êtes-vous sur de vouloir supprimer définitivement cette maintenance?');">
                <input type="hidden" name="_method" value="DELETE">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ maintenance_voile.id) }}">
                <button class="btn btn-danger">Supprimer cette maitenance</button>
            </form>
        </div>
    </div>
</div>

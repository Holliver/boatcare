<form method="post" action="{{ path('materiel_securite_legal_delete', {'id': materiel_securite_legal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ materiel_securite_legal.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

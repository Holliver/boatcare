{% extends 'base.html.twig' %}

{% block title %}Modifier : {{ voile.denomination }}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-6">
                <h4>Modifier : {{ voile.denomination }}</h4>
                <a href="{{ path('home') }}">Retour au bateau sans modifier la voile</a>
                {{ include('gestboat/home/voile/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
                {{ include('gestboat/home/voile/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
